import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://swapnilsingh.github.io',
  base: '/entitle-website',
  output: 'static',
});
